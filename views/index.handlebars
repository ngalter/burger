<div class="container">
<div class="row d-flex burger-head justify-content-center">
      <img src="../assets/img/burger.png" class="burger-img">
      <h1 class="burger-head-title">Eat-Da-Burger!</h1>
</div>
</div>
<div class="container">
  <div class="row d-flex burger-uneaten justify-content-center">
    <div class="col-md-6 ordered">
      {{!-- ordered but not eaten --}}
        <ul>
          {{#each burgers}}
            {{#unless devoured}}
                  <li>{{id}} {{burger_name}}  <button type="button" class="eat btn btn-warning" id ="eatbtn" data-id="{{this.id}}" data-newDevoured="{{devour}}">Devour It!</button></li>         
            {{/unless}}
          {{/each}}
        </ul>
    </div>
    <div class="col-md-6 unordered">
      {{!-- ordered and eaten --}}
      <ul>
  {{#each burgers}}
    {{#if devoured}}
        {{burgers}}
                    <ul>
                       <span><strong> {{this.id}} - {{this.burger_name}} </strong></span>
                    </ul>
        {{/if}}
        {{/each}}
    </ul>
    </div>
  </div>

</div>
</div>
{{!-- center bottom section --}}
<div class="row burger-foot">
<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="newBurger">Burger Name:</label>
    <input type="text" id="name" name="burger_name">
    <button type="submit" id="newBurger">Add Burger</button>
  </div>
</form>
</div>