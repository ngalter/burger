<div class="burger-head">
      <img src="../assets/img/burger.png" class="burger-img">
</div>
<div class="burger-head">
      <h1>Eat-Da-Burger!</h1>
</div>

<h2>Burgers that are ordered but not eaten.</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{!-- {{> burgers/burger-block devoured=true}} --}}
        {{burgers}}
          <li>{{id}} {{burger_name}}</li>

    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are ordered and Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
          <li>{{id}} {{burger_name}}</li>         
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label form="bn">Burger Name:</label>
    <input type="text" id="burg" name="burger_name">
    <button type="btn" id="eaten">Devoured!</button>
  </div>

  <button type="submit">Add Burger</button>
</form>
